# Cortexa Shared Packages

This directory (`packages/`) is reserved for shared libraries, cross-cutting contracts, and utility packages used across Cortexa applications (`apps/api`, `apps/web`, and future microservices/worker nodes).

## Structure
- `shared/`: Common TypeScript and Python type contracts, schema definitions, and constant registries.
